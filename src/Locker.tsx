type Locker = {
  service: number;
  statusinfoM: string;
  mlockerId: string;
  locker_Id: string;
  locker_size: string;
  status: string;
  Location?: string;
  location?: string;
  LockerLocation?: string;
  site?: string;
  // add other fields as needed
};
export type { Locker };
